# ───────── Server ─────────
PORT=8080
NODE_ENV=development

# ───────── MySQL ─────────
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=moltiz
MYSQL_PASSWORD=0715
MYSQL_DATABASE=moltiz

# ───────── Session ─────────
# 임의의 랜덤 문자열로 바꾸세요 (프로덕션에서는 더 길게/복잡하게)
SESSION_SECRET=아무랜덤문자열

# ───────── AI (선택: 키가 없어도 서비스는 동작, 있으면 의미검색 ON) ─────────
# OpenAI API 키가 있으면 넣으세요. 없으면 비워두세요.
OPENAI_API_KEY=sk-...           

# Q&A용 기본 모델(원하면 gpt-4o-mini 유지)
AI_MODEL=gpt-4o-mini

# 의미검색(자동완성)용 임베딩 모델
EMBED_MODEL=text-embedding-3-small

# 재인덱싱 보호 토큰 (임시)
# 브라우저에서 /api/ai/reindex?token=dev 호출할 때 필요
AI_ADMIN_TOKEN=dev


